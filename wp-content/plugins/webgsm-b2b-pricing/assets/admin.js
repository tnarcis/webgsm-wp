/**
 * WebGSM B2B Admin JS
 */
jQuery(document).ready(function($) {
    if (window.location.search.indexOf('webgsm_b2b_debug=1') !== -1 && window.console) {
        console.log('[WebGSM B2B] Admin loaded');
    }
});
